@page
@model SportClub.Soft.Areas.Participant.Pages.Participants.IndexModel

@{
    ViewData["Title"] = "Participant";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ParticipantData[0].Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParticipantData[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParticipantData[0].Age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParticipantData[0].Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ParticipantData) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
