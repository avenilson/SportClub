@page
@using SportClub.Pages.Extensions
@model SportClub.Soft.Areas.Participant.Pages.Participants.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ParticipantData</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ParticipantData.Id" />
            @Html.EditControlsFor(x => x.ParticipantData.Id)
            @Html.EditControlsFor(x => x.ParticipantData.Name)
            @Html.EditControlsFor(x => x.ParticipantData.Age)
            @Html.EditControlsFor(x => x.ParticipantData.Address)
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
